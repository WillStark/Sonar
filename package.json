{
  "name": "sonar",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "echo 'http://localhost:3000'",
    "demo:prepare": "node scripts/prepare-demo.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
